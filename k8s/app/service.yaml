# apiVersion: v1
# kind: Service
# metadata:
#   name: fastapi
# spec:
#   selector:
#     app: fastapi
#   ports:
#     - port: 80
#       targetPort: 8000
apiVersion: v1
kind: Service
metadata:
  name: fastapi
spec:
  selector:
    app: fastapi
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
